#ifndef MYSTL_H
#define MYSTL_H

#include <map>
#include <iostream>
#include <string>
#include <list>
#include <set>
#include <vector>

using namespace std;

int string_base(void);
int myVector(void);
/*
int mySet(void);
int myList(void);
void showmap(map<string, int> v);
int myMap(void);
*/
#endif